/// Framework identity stamped on outbound handshakes (OTLP scope, MCP client info) so the two can
/// never drift apart. Keep in sync with the release tag.
public enum AgentSquadInfo {
    public static let version = "0.1.0"
}
